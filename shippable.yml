language: none

build:
  pre_ci_boot:
    image_name: drydock/u14nod
    image_tag: prod
    options: "--net=host"

  ci:
    - docker run --net=host -p 5432:5432 -d postgres:9.5.2
    - while ! nc -z localhost 5432; do sleep 1; done
